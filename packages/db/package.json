{
  "name": "db",
  "version": "1.0.0",
  "main": "index.ts",
  "dependencies": {
    "@prisma/client": "^5.10.0"
  },
  "devDependencies": {
    "prisma": "^5.10.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "migrate": "prisma migrate dev",
    "push": "prisma db push",
    "studio": "prisma studio",
    "generate": "prisma generate"
  }
}
